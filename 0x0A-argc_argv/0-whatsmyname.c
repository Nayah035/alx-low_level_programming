#include <stdio.h>
#include "main.h"

/**
 * main - this is the main function.
 * @argc: culc the number of arguments.
 * @argv: this is an array of arguments.
 * Return: always zero on success.
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);
	{
	return (0);
	}
}
